import React from "react";
import { Button } from "semantic-ui-react";

const SearchButton = () => (
  <div>
    <Button style={{ marginTop: "3em" }} positive>
      İş ilanı bul
    </Button>
  </div>
);

export default SearchButton;
